# Session logs
`YYYY-MM-DD.md` per day — appended per turn: time, speaker match, language, user text, Atlas reply,
tools used, emotion state, and the timing line (wake/ASR/TTFT/TTS-first-audio).
This is your debugging substrate *and* your personal history. Never committed if you mark it `atlas: private`.
